{
  "name": "own-nest",
  "version": "1.0.0",
  "description": "my own backend projects",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "start:dev": "npm run build && npm run start"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "reflect-metadata": "^0.2.2",
    "typescript": "^5.5.3"
  },
  "private": true
}
